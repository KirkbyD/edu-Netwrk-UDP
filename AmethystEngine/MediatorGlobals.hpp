#pragma once
// enums for the mediator communications these should all be externally defined in the mediator class
extern enum cmdSubsystem;	// SUBSYSTEM TYPE
extern enum cmdType;		// CMD TYPE
extern enum cmdErrorType;	// ERROR TYPE
